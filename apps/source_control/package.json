{
    "$schema": "https://json.schemastore.org/package.json",
    "name": "@porti/source-control",
    "version": "0.0.1",
    "scripts": {
        "build": "cargo build",
        "test": "cargo test",
        "run": "SOURCE_CONTROL_CONFIG_PATH=config.local.json cargo run"
    }
}
